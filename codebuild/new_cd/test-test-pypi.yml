# Assumes are running using the Ubuntu 16.04:x64 image
# Runs fourth - makes sure the release to test PyPi repository worked
version: 0.2
phases:
  install:
    commands:
      - sudo apt-get update -y
      - sudo apt-get install python3 python3-pip -y
      - python3 -m pip install --upgrade pip
      - python3 -m pip install --upgrade setuptools

  pre_build:
    commands:
      # - curl https://www.amazontrust.com/repository/AmazonRootCA1.pem --output /tmp/AmazonRootCA1.pem
      # - cert=$(aws secretsmanager get-secret-value --secret-id "unit-test/certificate" --query "SecretString" | cut -f2 -d":" | cut -f2 -d\") && echo "$cert" > /tmp/certificate.pem
      # - key=$(aws secretsmanager get-secret-value --secret-id "unit-test/privatekey" --query "SecretString" | cut -f2 -d":" | cut -f2 -d\") && echo "$key" > /tmp/privatekey.pem
      # - ENDPOINT=$(aws secretsmanager get-secret-value --secret-id "unit-test/endpoint" --query "SecretString" | cut -f2 -d":" | sed -e 's/[\\\"\}]//g')
  build:
    commands:
      - echo Build started on `date`
      - cd $CODEBUILD_SRC_DIR/aws-iot-device-sdk-python-v2

      # TODO: Pipeline does not (currently) make a release, so we cannot test it...
      # - CURRENT_TAG_VERSION=$(cat VERSION)
      # # this is here because typing isn't in test-pypi, so pull it from prod instead
      # - python3 -m pip install typing
      # - python3 codebuild/new_cd/pip-install-with-retry.py -i https://testpypi.python.org/simple --user awsiotsdk==$CURRENT_TAG_VERSION

      # TODO run basic discovery sample?
      # - python3 samples/basic_discovery.py --region us-east-1 --cert /tmp/certificate.pem --key /tmp/privatekey.pem --ca_file /tmp/AmazonRootCA1.pem --thing_name aws-sdk-crt-unit-test --print_discover_resp_only --verbosity Trace

  post_build:
    commands:
      - echo Build completed on `date`
